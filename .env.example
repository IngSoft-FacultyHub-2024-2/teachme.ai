# Application Configuration
NODE_ENV=development

# Developer instructions template used by OpenAI calls when not provided programmatically.
# Put short guidance here. KEEP IT SAFE (no secrets).
DEVELOPER_INSTRUCTIONS="You are an expert TypeScript assistant. Be concise and always explain reasoning when asked. Keep code examples minimal and runnable."

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4-turbo-preview
OPENAI_MAX_TOKENS=4096
OPENAI_TEMPERATURE=0.7

# Token Limits & Warnings
OPENAI_CONTEXT_WINDOW=128000
OPENAI_WARNING_THRESHOLD=0.8

# Optional: use a reusable prompt template managed by the OpenAI Responses API for kata solver
# If set, OPENAI_PROMPT_ID should be the prompt resource id and OPENAI_PROMPT_VERSION its version.
# Example:
# OPENAI_PROMPT_ID=pmpt_123456789abcdef
# OPENAI_PROMPT_VERSION=1

# Code Extractor Configuration
# Use a reusable prompt template managed by the OpenAI Responses API for code extraction
# CODE_EXTRACTOR_PROMPT_ID should be the prompt resource id and CODE_EXTRACTOR_PROMPT_VERSION its version.
# Example:
# CODE_EXTRACTOR_PROMPT_ID=pmpt_987654321fedcba
# CODE_EXTRACTOR_PROMPT_VERSION=1

# Kata Evaluator Configuration
# Model to use for code evaluation (can be different from main OPENAI_MODEL)
KATA_EVALUATOR_MODEL=gpt-4-turbo-preview
# Maximum tokens for evaluation response
KATA_EVALUATOR_MAX_TOKENS=4096
# Optional: use a reusable prompt template managed by the OpenAI Responses API for kata evaluation
# KATA_EVALUATOR_PROMPT_ID should be the prompt resource id and KATA_EVALUATOR_PROMPT_VERSION its version.
# Example:
# KATA_EVALUATOR_PROMPT_ID=pmpt_evaluator123
# KATA_EVALUATOR_PROMPT_VERSION=1

# Kata File Configuration
# Directory containing kata instruction and rubric files
KATA_INPUT_DATA_PATH=./src/inputData
# Default kata instruction file to load on startup
KATA_DEFAULT_INSTRUCTION_FILE=kata-instructions.json
# Default evaluation rubric file to load on startup
KATA_DEFAULT_RUBRIC_FILE=kata_evaluation_rubric.json

# Optional: path to a file containing developer instructions (fallback if DEVELOPER_INSTRUCTIONS is empty)
# DEVELOPER_INSTRUCTIONS_FILE=./src/features/kata-solver/developer_instructions.md

# API Configuration
# API_KEY=your_api_key_here
# API_ENDPOINT=https://api.example.com

# Database Configuration (if needed)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Feature Flags
# ENABLE_FEATURE_X=false

# Service Configuration
# TIMEOUT_MS=5000
# MAX_RETRIES=3
